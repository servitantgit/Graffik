{
  "name": "graffik",
  "private": true,
  "description": "Grafik Gillette — factory shift PWA (no runtime npm deps)",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "lint": "eslint js/ --max-warnings 0",
    "lint:fix": "eslint js/ --fix",
    "check": "npm test && npm run lint"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "eslint": "^9.21.0"
  }
}
